#[macro_use]
extern crate tracing;
pub mod modbus;
pub mod sunspec;
pub mod sunspec_data;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
